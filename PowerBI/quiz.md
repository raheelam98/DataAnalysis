# Power BI Quiz 1 

#### 1. What's the most powerful feature of Power BI?
-  Collaboration and sharing  
-  Data connectivity and integration  
-  Interactive dashboards  
-  Data modeling and DAX formulas  

#### 2. What's the most important aspect of data visualization?
-  Interactivity and exploration  
-  Aesthetics and design  
-  Accuracy and precision  
-  Telling a story with data  

#### 3. What's the purpose of CALCULATE in DAX?
-  To create a new table  
-  To define a calculated column  
-  To filter or modify the filter context for a calculation  
-  To load data from a source  

#### 4. Which DAX function returns a distinct value in a column?
-  DISTINCT()  
-  FILTER()  
-  ALL()  

---

# Power BI DAX Quiz

This quiz covers key concepts of DAX (Data Analysis Expressions) in Power BI.

---

#### 1. What is DAX in Power BI?
- A database management tool  
- A visual design framework  
- A formula language used for data calculations and analysis  
- A data modeling diagram  

---

#### 2. What is the main difference between a DAX Calculated Column and a DAX Measure?
- Measures are stored row by row; columns are aggregated  
- Calculated columns are computed per row; measures are aggregated values  
- Measures take more memory than calculated columns  

---

#### 3. What is an explicit measure in Power BI?
- A measure automatically created by Power BI  
- A measure written manually using DAX  
- A calculated column  
- A quick measure  

---

#### 4. Which of the following best describes the purpose of using DAX in Power BI?
- Connecting to data sources  
- Modeling relationships  
- Creating custom aggregations and logic  
- Designing report layouts  

---

#### 5. What is TRUE about M Query and DAX?
- M Query transforms data before loading into Power BI  
- M Query is used in Power Query Editor while DAX is used in Power BI  
- DAX performs calculations after data has been loaded into the model  
- All of them  

---

What is DAX in Power BI?

A database management tool
A visual design framework
A formula language used for data calculations and analysis
A data modeling diagram

What is the main difference between a DAX Calculated Column and a DAX Measure?

Measures are stored row by row; columns are aggregated
Calculated columns are computed per row; measures are aggregated values
Measures take more memory than calculated columns

What is an explicit measure in Power BI?

A measure automatically created by Power BI
A measure written manually using DAX
A calculated column
A quick measure

Which of the following best describes the purpose of using DAX in Power BI?

Connecting to data sources
Modeling relationships
Creating custom aggregations and logic
Designing report layouts
 

What is TRUE about M Query and DAX?

M Query transforms data before loading into Power BI
M Query is used in Power Query Editor While DAX in Power BI
DAX performs calculations after data has been loaded into the model
All of them



---

### Power BI Quiz 1 Answers 

#### 1. What's the most powerful feature of Power BI?
**Answer:** Data modeling and DAX formulas

#### 2. What's the most important aspect of data visualization?
**Answer:** Telling a story with data

#### 3. What's the purpose of CALCULATE in DAX?
**Answer:** To filter or modify the filter context for a calculation

#### 4. Which DAX function returns a distinct value in a column?
**Answer:** DISTINCT()

